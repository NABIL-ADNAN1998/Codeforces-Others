#include <bits/stdc++.h>
using ll = long long int;
#include <algorithm>
using namespace std;

int main()
{

    string a;

    cin >> a;
    for (ll i = 0; i < a.length(); i++)
    {
        cout << a[i];
    }
    for (ll i = a.length() - 1; i >= 0; i--)
    {
        cout << a[i];
    }
}
