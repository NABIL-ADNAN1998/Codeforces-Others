class Solution:
    def isPalindrome(self, x: int) -> bool:
        ans = True
        y = str(x)
        l = 0
        r = len(y)-1
        while (l < r):
            if (y[l] != y[r]):
                ans = False
                return ans
            l += 1
            r -= 1
        return ans
