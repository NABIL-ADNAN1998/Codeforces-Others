#include <bits/stdc++.h>
using namespace std;
using ll = long long int;

void solve()
{
    ll k, l, m, n, d, count = 0;
    cin >> k >> l >> m >> n >> d;
    for (ll i = 1; i <= d; i++)
    {
        if (i % k == 0 || i % l == 0 || i % m == 0 || i % n == 0)
        {
            count++;
        }
        else
        {
            continue;
        }
    }
    cout << count << endl;
}

int main()
{
    /* ll test;
     cin >> test;

     while (test--)
     {
         solve();
     }
     */
    solve();
}
