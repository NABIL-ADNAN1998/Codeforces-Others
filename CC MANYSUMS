

#include <bits/stdc++.h>
using namespace std;
using ll = long long int;
void solve()
{
    ll a, b;
    cin >> a >> b;
    cout << ((2 * b) - (2 * a) + 1) << endl;
}

int main()
{
    ll test;
    cin >> test;
    while (test--)
    {
        solve();
    }
}
